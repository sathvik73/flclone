{
    "name": "amazon-clone-root",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "cd server && npm install",
        "build": "cd client && npm install && npm run build"
    },
    "engines": {
        "node": "18.x"
    }
}